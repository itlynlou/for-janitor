# Get a free key at https://opencode.ai/auth (no credit card required).
# This key stays on your server — Janitor AI never sees it.
OPENCODE_ZEN_KEY=your_opencode_zen_key_here

# Optional, defaults to 3000
PORT=3000
